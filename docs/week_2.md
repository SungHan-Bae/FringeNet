# Week 2 실험 노트 — 2026-08-15 ~ 2026-08-21

날짜별 진행·발견·결정의 연표. 방법과 수치의 근거는 `reports/<실험>.md`에 있고, 여기에는
**무엇을 왜 그렇게 정했는지**만 남긴다. Week 1(스캐폴드 ~ Stage B 물리 손실 종결)은
[week_1.md](week_1.md). 수치 정본은 CLAUDE.md 「모델·학습 스펙」과 [reports/](../reports/)다.

---

## 08-15 (토) — 방향 확정: 학습 레시피로 CNN을 밀고 뒤에 LM을 붙인다

**목표를 판정 가능한 숫자 둘로 옮겼다.** 주장은 "파라미터·학습비용을 훨씬 덜 쓰고 213M 단독과
동급 이상"이다.

- **성능선 = post-LM MAE ≤ 0.3955 nm** (skip-MLP 단독). 현재 0.6110, 천장 0.3336.
  오차 구조가 `post-LM ≈ 0.33 + 분지실패율 × 약 40 nm`이므로(십분위 1~9가 0.25~0.47로 평평,
  10분위만 3.205) **목표는 CNN MAE가 아니라 분지 실패율 0.67% → 약 0.15%다.** run마다
  5,000행에 LM을 붙여 실패율로 판정한다.
- **자원선은 세 미터로 나눠 잰다**: 파라미터·크기(322배 유리) · 학습 비용(에폭당 12.0초 vs
  119.6초) · 추론 지연. 앞의 둘은 이미 이기고 있었고 **지연만 지고 있었다** → 오늘 그 자리를
  고쳤다(아래).
- **용량은 유지하거나 조금만 늘린다** — 예산·레시피가 먼저다. 근거는 과소적합이 측정돼 있다는
  것이다: CNN은 `train_l1` 2.1943 / val 2.3455(격차 0.151)이고 skip-MLP는 0.1529 / 0.3955다.
  둘 다 **best 에폭이 마지막이고 lr이 0으로 소진돼** 끝났다.
- **비교 대상은 213M 단독으로 고정한다.** 213M에 같은 LM 보정을 붙이는 대조는 하지 않기로
  했으므로, "큰 모델에도 붙이면?"은 **측정하지 않은 열린 질문**으로 리포트에 남긴다.

### 추론 지연 레버 — 해석적 야코비안 + 조기 종료

디코더가 미분가능한데 역해가 중앙차분을 쓰고 있었다. M_j = cos δ_j·I + i sin δ_j·K_j 이고
K_j² = I 이므로 **dM_j/dd_j = i·k_j·K_j·M_j** 다. 접두 곱과 접미 벡터를 한 번씩 모으면
cos/sin을 다시 계산하지 않고 L개 열을 전부 얻는다 (`tmm_reflectance_jacobian`).

- **측정 (로컬 WSL2 CPU 8스레드, holdout 4,096행)**: LM 30회 c128이 11.764 → 3.638(해석적,
  3.23배) → 1.618(+조기종료, 7.27배) → c64에서 1.084(**10.85배**) ms/행. **MAE는 0.6752로
  전부 동일하다.** `cnn+LM` 합계가 12.060 → 1.380 ms/행이라 skip-MLP 대비 **5.53배 손해에서
  0.63배(1.6배 유리)로 뒤집힌다.**
- **🔴 조기 종료 기준을 잘못 잡으면 정확도를 잃는다.** 기각된 스텝을 이동 0으로 세니 MAE가
  +0.0093 nm 나빠졌다 — 감쇠가 오르며 스텝이 되살아나는 LM 특성 때문에 어려운 행이 잘린다.
  **제안된 스텝 폭**으로 판정하니 대가가 0이 됐다.
- **🔴 부수 소득 — dtype 흔들림이 사라진다.** c64 중앙차분은 MAE 0.6503으로 c128(0.6752)과
  다르다. 보폭 1e-3 nm가 상대 섭동 1e-5라 float32 유효자리(1.2e-7)를 먹기 때문이고, 이 표본에서
  낮게 나온 것은 메커니즘이 아니라 우연이다. 해석적 경로는 c64가 c128과 같은 값을 준다.
- **🔴 R을 접미 벡터에서 만들면 forward와 비트가 어긋난다** (곱 순서가 반대). LM이 두 경로의 R을
  같은 잔차 장부에 섞어 쓰므로 **접두 곱에서 만들어** 비트 동일을 테스트로 걸었다. 정확성은
  **autograd로** 검증(상대오차 1.2e-14, 물리 테스트 8번 — L=1도 따로 건다). 기본값 전환은
  정본 재생성과 함께 08-18에 했다.

## 08-17 (월) — 레시피 라운드 1 결과: 레버는 예산 하나였다

5런 완주(100에폭 × 5, 합계 103분), Drive 무결성 검증 5/5, 런타임 자동 반납. 정본은
`runs/cnn_recipe/*/metrics.json`과 [reports/cnn_recipe.md](../reports/cnn_recipe.md).

- **예산이 유일한 레버다.** 에폭 30 → 100으로 val 2.3455 → 1.7185 nm, post-LM 0.6643 → 0.4962,
  분지 실패율 0.78% → 0.42%. 레시피 손잡이 4종은 **하나도 이득이 없다** — 입력 표준화가
  2.4σ 순손해고 EMA·노이즈·꼬리는 잡음 안이다.
- **🔴 Δ를 사슬 부모 대비로 읽어 한 번 잘못 읽었다.** `+tail`이 부모 대비 −0.0884라 "유일한
  이득"으로 보였는데, 부모(`+std+ema`)가 이미 표준화로 깎여 있었다. **순수 `budget100` 대비로는
  −0.0098 = 0.3σ로 동점**이다. → **사슬 실험의 Δ는 부모가 아니라 대조군 기준으로 읽는다.**
- **표본 잡음을 먼저 구했어야 했다.** post-LM은 소수 실패 행이 지배해서 5,000행 표본의 1σ가
  ±0.037 nm다(실패 21행 → 포아송 ±4.6행 × 40/5000 nm). 이 값 없이는 −0.0098과 +0.0874를 같은
  자격으로 읽게 된다.
- **🔴 병목이 최적화에서 용량으로 옮겨갔다.** 일반화 격차가 30에폭 0.151 → 100에폭 **0.243**이고
  이는 skip-MLP의 0.243과 **같다**. 정규화·최적화 여지는 소진됐고 남은 차이는 순수 적합력이다 —
  `train_l1` 1.4754가 이 백본의 벽이다.
- **🔴 분지 실패의 74%는 라벨 없이 검출된다.** 재구성 잔차 상위 0.5%(25행)에 실패의 64%가
  정밀도 **100%**로 들어 있고, 1%에서 74.4%까지 오른 뒤 평평하다. 남는 26%는 관측을 거의 같게
  설명하는 **등가 분지**라 잔차로 구분되지 않는다(오차 중앙값 13.4 nm vs 전체 실패 37.8 nm).
  → 다음 수가 재훈련이 아니라 **표적 재시도**로 정해졌다.
- 판정 측정을 `scripts/judge_recipe.py`로 정본화했다 (**새 수치를 주장하려면 스크립트를
  남긴다** 규약이 걸린 자리 — 기준 팔 표본 수치에 재현 경로가 없었다). λ 스윕 라운드는
  꼬리 손실 동점으로 전제가 사라져 걷어냈다.

### 되돌림 규칙 — 학습 없이 post-LM −29% (같은 날)

발견 ⑥을 방법으로 옮겼다. `flag_unreliable`(`src/physics/invert.py`)이 재구성 잔차의
중앙값 + k·MAD를 문턱으로 잡아 "이 답은 관측을 설명하지 못한다"는 행을 지목하고, 그 행에서만
LM 보정을 버리고 CNN 예측을 쓴다. **전체 holdout 81,000행에서 post-LM 0.6110 → 0.4336 nm
(−29.0%)**, 지목 433행 중 적중 430(정밀도 99.3%). 정본은 `reports/cnn_recipe_judge.md`.

- **라벨이 어디에도 안 들어간다.** 지목은 관측 스펙트럼과 우리 답만 쓰고, 문턱은 잔차 분포
  자체에서 나온다(행의 99% 이상이 정상이므로 robust 통계가 정상 분포를 잡는다). 라벨은
  "이 방법이 되는가"를 채점할 때만 썼다 — 계기를 한 번 교정하고 그 뒤로는 기준 없이 쓰는 것과 같다.
- **이 한 규칙이 레시피 라운드 전체보다 이득이 크다** (−0.177 vs 예산 −0.168).
- **왜 되는가**: 실패 행에서 CNN 오차 11.27 nm인데 LM이 37.76 nm로 만든다 — LM은 잘못된 골짜기
  바닥까지 성실하게 내려간다. 그 행에서는 보정을 안 하는 게 낫다.
- **τ 규칙과 다른 점을 분명히 해야 한다.** 예전에 기각된 이동 상한 τ는 선택자가 **이동 거리**였고
  문턱을 holdout에서 골랐다. 지금은 선택자가 **잔차**(정밀도 99.3% 측정)이고 문턱이 잔차 분포에서
  나온다. **k는 사전등록한다** — MAE 보며 고르면 같은 함정이다.
- 실패 **건수**는 0.67% → 0.64%로 거의 그대로다. 줄어든 것은 심각도다 — 건수를 줄이려면 올바른
  분지를 찾아야 하고 그게 다중 시작의 몫이다.
- 해석적 야코비안 경로가 전체 holdout에서 커밋된 정본을 **4자리까지 재현**했다
  (post-LM 0.6110, 실패 0.67% — `reports/inversion_refine.md`와 일치). 2분 16초, CPU 8스레드.

### 🔴 `budget100` 확정 — 322배 작은 모델이 213M 단독을 넘었다

Drive 미러에서 `budget100`의 model.pt를 회수해 재추론으로 무결성 확인(holdout 1.7185 nm,
층별까지 `metrics.json`과 일치) 후 전체 holdout 81,000행에서 측정했다.

| | post-LM | 되돌림 후 |
|---|---|---|
| 기준 (30에폭) | 0.6110 | 0.4336 |
| **`budget100`** | 0.4884 | **0.3880 nm** |
| 213M skip-MLP 단독 | — | 0.3955 nm |

- **0.3880 < 0.3955.** 같은 holdout·같은 split(val_frac 0.1, seed 42)이고 추론이 결정론적이라
  **이 평가셋에서는 표본 오차 없이 확정적이다.** 지목 235행 중 적중 232(정밀도 98.7%).
- **자원 미터 네 축을 모두 이겼다**: 파라미터 322배 · 크기 325배 · 학습 9.7배 · 추론 1.6배 ·
  정확도 1.9%. 용량을 건드리지 않았으므로 파라미터 주장이 온전하다.
- **우위가 0.0075 nm(1.9%)로 얇고 holdout은 격자 위 조합 보간이다** — 격자 밖에서 유지되는지는
  리더보드 제출로만 답이 나온다. 다음 할 일이 그것이다.
- 오차 구조 모형이 `budget100`에서 거의 정확히 맞는다 (실측 0.4884 vs 모형 0.4865, +0.0019).
- 고정 백분위 검출(상위 0.5%)은 실패가 적은 모델에서 정밀도가 떨어져 보인다(58.0%) — 실패
  316행보다 후보 405행이 많아서다. **σ 기반 문턱은 스스로 적응한다**(235행 지목, 98.7%).

### 제출 경로 배선 — 라벨 없이 격자 밖 전이를 봤다 (같은 날)

배포 합성을 `refine_with_fallback`(`src/physics/invert.py`)으로 올리고 `src/evaluate.py`에
`--refine`을 붙였다. **판정 스크립트와 배포가 같은 함수를 쓴다** — 두 곳에 따로 두면 조용히
갈라진다. `python -m src.evaluate --run runs/cnn_recipe/budget100 --submission --refine` 한 줄로
holdout 확정 수치와 test 보정본이 함께 나온다.

- **test는 라벨이 없어도 잔차 분포는 비교할 수 있다.** post-LM 잔차 중앙값이 holdout 0.007829 vs
  test 0.007839(**+0.1%**)이고 지목률도 0.29% vs 0.28%다 → 몸통이 그대로 전이된다.
  **다만 p99.9가 0.059 → 0.119로 2배**다 — 격자 밖 외삽의 대가가 극단 꼬리에 몰리고, post-LM
  MAE는 그 꼬리가 지배한다. 잔차는 "관측을 설명하는가"만 재므로 **MAE 주장의 근거는 아니다**.
- **🔴 범위 클리핑의 holdout 이득은 격자 인공물이다.** [10, 300] 클리핑이 holdout을
  0.3880 → 0.3804로 만드는데, holdout은 **참 두께의 6.71%가 격자 끝에 쌓여** 있어서다(30값 중
  2개). test는 연속이라 범위 밖 해가 0.06%뿐이고 이득이 없다. → `--refine-clip`은 opt-in,
  기본 제출은 클리핑 없음. 「하지 말 것」에 규칙으로 넣었다.
- 제출 csv는 git 미추적으로 두었다 (`.gitignore`) — 한 줄로 재생성되고 test id가 섞인 산출물을
  저장소에 두지 않는다.

## 08-18 (화) — 전수 감사 후 보수 시작: 위생·원천 수정 (fix/audit-hygiene)

레포 전수 감사(수치 정합성·산출물 관리·문서 규약·포트폴리오 관점)에서 치명 0건을 확인했고,
발견된 스테일·중복·죽은 가드를 단계별 브랜치로 해소했다. 백로그에 **모델·학습 최적화를
새 Task 8**로 삽입하고 문서화는 Task 9로. **다중 시작 재탐색은 하지 않기로 결정** —
백로그에서 도려낸다.

### 수치 정본화 — 손계산 수치 전부에 산출 경로가 생겼다 (같은 날, exp/canonical-numbers)

되돌림 근거(지목 행 CNN vs LM)를 judge_recipe가 직접 내게 하고, level1_cnn 진단 수치를
`diagnose_predictions.py`로 재현했다. 문서가 인용해 온 손수치 두 건이 정본과 어긋나
있었다(되돌림 근거 11.27/37.76 → **13.59/48.22**, 여유 임계 위반율 3.26% → **3.21%**) —
손계산 금지 규약이 왜 있는지의 실증. 기존 수치 행은 전부 재생성 diff 0.

### 실측 축 완결 — 평가 축·벤치 CPU 완전판·격자 밖 반증 (같은 날, exp/eval-axes)

- **평가 축 실측 완료** (`reports/cnn_recipe_axes.md`): 노이즈 강건성은 균등·가우시안이
  같은 총 σ에서 거의 같은 열화(주입 ±0.030에서 budget100 1.72 → 3.05 nm) — 종류가 아니라
  크기가 지배한다. 신뢰도 지표 ρ 0.70, 잔차 상위 10% MAE 4.97 vs 하위 0.60 → 라벨 없는
  이상 감지 축 성립.
- **inversion_bench CPU 절을 로컬 완전판으로 교체** (해석적·조기종료 축 포함): 해석적+
  조기종료 c64에서 `cnn+LM` 1.005 ms/행 = skip-MLP의 **0.61배** (중앙차분은 6.55배 손해).
  GPU 절은 L4 중앙차분 판본 보존 — 완전판 노트북(round2) 준비됨, Colab 실행 대기.
- **`verify_data.py --deep` 신설**: test 격자 밖 반증이 커밋된 스크립트 경로로 재현된다
  (표본 64행 전부 임계 0.0304의 1.6~4.6배).

### 리포트 다듬기 + 백로그 개편 (같은 날, docs/report-trim)

**`reports/stage_b.md` 취합 신설** (β 기각의 세 라운드 관통 서사 — 핵심 문장: "손실 항은
CNN에게 이미 할 줄 아는 일을 시켰다"). 정본표 복제를 리포트 4편에서 제거하고 문서 수치를
산출물에 정렬. 백로그 개편: 신설 **Task 8 = 모델·학습 최적화**(기각 손잡이 4종 재시도 금지
명시), 문서화 → **Task 9**.

### 🔴 리더보드 제출 — test MAE 0.38733, 격자 밖 확인 완료 (같은 날)

`submission_budget100_refined.csv`를 데이콘에 제출했다 (팀 감자맛탕, 전체 15위). **holdout
0.3880 → test 0.38733 (−0.2%)** — 격자 밖 외삽에서 열화가 사실상 0이다. 우려했던 잔차 극단
꼬리 2배(p99.9)는 소수 행이라 MAE를 움직이지 못했고, 라벨 없는 전이 예측(몸통 그대로)이
채점으로 확인됐다. holdout에서 확정한 파이프라인을 수정 없이 한 번 제출한 결과다. 이로써
**Task 7 종결.** 남은 열린 질문 하나: 213M skip-MLP와의 격자 밖 직접 비교는 미측정이다
(skip-MLP 예측은 제출하지 않았다). 기록은 `reports/cnn_recipe.md` «리더보드 확정».

### 추론 벤치 완전판 (L4) + 해석적 야코비안 기본값 전환 (같은 날, task7/bench-round2-gpu)

round2 노트북을 Colab L4에서 Run-All — `reports/inversion_bench.md`가 한 세션의
CPU(VM)·GPU(L4) 절을 담은 완전판이 됐다 (무정지 실행·push·자동 반납, 실행 로그 커밋).

- **GPU에서도 해석적+조기종료가 성립한다.** L4에서 LM 중앙차분 c128 0.815 → 해석적+조기종료
  c64 0.054 ms/행(15배), `cnn+LM` 합계가 skip-MLP forward 대비 **17.41배 손해 → 1.30배**.
  MAE는 c128 세 변형과 c64 해석적+조기종료가 **0.6630으로 전부 동일** — dtype 흔들림 소멸까지
  GPU에서 재확인. 반복수 축소(10회)는 여전히 열등(+0.033 nm, 속도도 짐).
- **🔴 "추론 1.6배 유리"는 WSL2 한정이었다.** 새 정본 기준 Colab CPU 1.18배 · L4 1.30배
  **느림** — 배수는 기계에 따라 승패가 뒤집힌다("단일 배수 인용 금지"의 실증). 자원 미터
  추론 축을 "동급"으로 정정했다 (`cnn_recipe.md`).
- **기본값 전환**: `lm_invert`·`refine_inversion.py` 기본을 analytic+조기종료로.
  `inversion_refine.md` 전체 holdout 재생성이 **수치 전 항목을 재현**했고(0.6110 / 77.6814 /
  0.3336 — 7분, fd 55분 대비), 게이트 (d)는 **fd 명시 고정**(PhysicalStack에 해석적 경로
  없음 — 새 기본값이 TypeError로 시끄럽게 드러냈다) 후 `stage_a_gate.md` 재생성 **diff 0**.

### 진열장 재편 — README·인덱스·헤드라인 그림 (같은 날, docs/readme-refresh)

- **README 재편**: §1·§3.2가 기각된 물리 손실을 현행 핵심으로 서술하던 것을 실제 결론
  ("결합의 자리가 핵심 발견 — 손실은 기각, 추론 시 역산+되돌림이 채택")으로 교체하고
  "물리의 값어치" 소절 신설. §2.1 모순 제거, §3.3 테스트 표 8종, §4 트리 전면 갱신,
  §5에 최종 제출 커맨드. 상단에 헤드라인 그림 임베드 (프로즈 수치는 계속 0 — 규약 유지).
- **`reports/README.md` 인덱스 신설**: 진행 비교표(단일 위치)·읽기 순서 7편·정본/취합
  구분. 헤드라인 그림은 `make_headline_figure.py`가 metrics.json·judge json에서 읽어
  산출 — 손 수치 0이라 수치 갱신 시 재실행만으로 따라온다. 취합 리포트 4종에 그림 임베드.
- **week_1 축약** (519→329줄): 08-14 블록의 분석 서사를 stage_b.md 등 정본으로 위임하고
  결정 중심 연표로. «확정 수치» 표(이미 스테일)는 제거하고 정본 포인터로 대체.
- **docs/README.md 삭제** — 두 번의 정리 사이클을 살아남지 못한 인덱스. 로드맵 한 줄은
  루트 README §6로 이관, 리포트 안내는 reports/README.md가 맡는다.
- stage_a 예외 추적 근거 수치 44 KB → 실측 약 172 KB (3곳).

## 08-19 (수) — Task 8 착수: 라운드 1 설계 (task8/arch-round1)

- **후보 선별**: 최신/표준 기법을 조사해 이 문제에 맞는 것만 추림. **채택 순서** — 구조
  (잔차·ConvNeXt-1D) → 용량(폭·깊이) → 부착 모듈(rFFT 분기·SE 어텐션, 라운드 2 후보) →
  옵티마이저(Muon, 큰 모델과 짝지을 때만). **탈락과 사유** — Huber/MSE(= 기각된 꼬리 가중
  `budget100-std-ema-tail`의 L1+0.1·MSE와 같은 계열), U-Net(dense 출력용 — 벡터 회귀에
  디코더 절반 낭비), 다중 커널(수용영역 다중 스케일은 dilation으로 이미 백본에 있음),
  soft binning/label smoothing(격자 누설 계열), Transformer(파라미터 서사·기간 리스크).
- **라운드 1 = 구조 2 + 용량 4** (사슬 아님, Δ는 budget100 대조): `CNN1D`에 `residual`
  플래그(MLPBlock.skip 규약의 conv 판 — identity 또는 1×1 projection), `ConvNeXt1D` 신규
  (depthwise + inverted bottleneck + layer scale, stem stride 1·flatten head로 이 태스크에
  맞춤). match 2종은 파라미터 매칭(664,780 / 641,972 — MLP ±10% 규약 테스트로 고정),
  w2/d2는 폭 ×2(×3.9)·깊이 ×2(×1.9~2.3). 사전등록·판정 규약은 노트북 헤더
  (`notebooks/task8/round1_arch.ipynb`)에 — 반증 조건(표본 잡음 0.037 이상 못 이김)이
  성립하면 구조·용량 레버 소진으로 기록하고 부착 모듈로 간다.

### 라운드 1 실행 — 구조 2 run으로 범위 축소 후 완주 (같은 날, Colab L4)

- **범위 축소 (실행 중 결정)**: ConvNeXt **80초/에폭 실측** (resnet 17.6초) — 용량 4 run
  동반 시 세션 예산 초과라 구조 2 run으로 줄이고 용량은 라운드 2로 분리. 승자 선택
  사다리를 결과 확인 전에 사전등록: ① post-LM(±0.037 이내 동률) ② train_l1 ③ 에폭당 비용.
- **결과 (대조군 budget100: val 1.7185 / train_l1 1.4754 / post-LM 5천행 0.4962)**:
  - `resnet-match` val **1.6483**(−0.070, 전 층 균일 개선) / train_l1 **1.4033** —
    **매칭 파라미터에서 구조만으로 벽을 뚫었다** (−4.9%). 사전등록 1 부합.
  - `convnext-match` val 1.8108(+0.092) / train_l1 1.5642 — fit·val 모두 열세 + 4.8배 비쌈.
    layer scale 1e-6의 항등 출발이 100에폭 예산에 무거운 것으로 해석.
  - **경고: val 이득이 post-LM으로 옮겨가지 않았다** — resnet post-LM +0.0662(≈1.8σ),
    실패 21→26행(포아송 ±4.6 이내). 균일 정밀도 이득은 분지 실패를 안 건드린다.
    사전등록 3(적합↑→실패↓)은 표본에서 어긋남 → 전체 holdout 판정으로 확정
    (`reports/task8_judge.md`, 표본 오차 0).

### 전체 holdout 판정 — 표본 경고는 잡음이었고, 두 구조 모두 최선을 넘었다 (같은 날, 로컬)

- 정본은 `reports/task8_judge.md` (81,000행, 표본 오차 0 — budget100 0.4884/0.39%가 정확히
  재현돼 무결성 겸 확인). **표본(5천행)의 "resnet post-LM +0.066 악화"는 잡음이었다** —
  전체에서는 resnet 0.4709/0.34%, convnext 0.4690/0.34%로 **둘 다 대조군을 이긴다**.
  post-LM 순위를 표본으로 읽지 않는다는 규칙의 재실증.
- **되돌림 후 resnet 0.3838 / convnext 0.3803 — 매칭 0.66M에서 이미 프로젝트 최선
  0.3880을 넘었다** (용량 확장 전, 구조만으로). 채택 결정은 Task 8 종료 시(라운드 2 결과와
  함께) — 진행 비교표·헤드라인은 그때 일괄 갱신.
- **승자 사다리 적용 → 라운드 2 = resnet**: ① post-LM 차 0.0019는 재현 잡음(실패 ~275행
  포아송 ±17 ≈ ±0.008 nm) 이내 동률 ② train_l1 resnet 1.4033 vs 1.5642 ③ 비용 17.6초 vs
  80초/에폭. convnext의 "val 최악·post-LM 최선" 갈림은 judge 리포트의 존재 이유 재확인.

### 라운드 2 (용량) — 깊이 압승, 되돌림 후 0.3412로 최선 경신 (같은 날, Colab L4 + 로컬 판정)

- **resnet-d2 (블록 5→10, 1.51M)**: val **0.3647** / train_l1 **0.3071** — 벽 1.4033이
  무너졌다(−78%). 전체 holdout post-LM **0.3430** / 실패율 **0.08%** / 되돌림 후 **0.3412**
  (기존 최선 0.3880 대비 −12%, 213M 단독 0.3955 대비 −14%). 일반화 격차 0.058 (match 0.245).
- **resnet-w2 (채널 ×2, 2.62M)**: val 1.1515 — 개선은 했으나 d2에 3배 뒤진다. **깊이 ≫ 폭**:
  42% 적은 파라미터로 파라미터당 이득이 압도적. 213M skip-MLP(폭 방향)와 대조되는 발견.
- **물리 보정의 한계 기여가 붕괴 중** — LM+되돌림의 Δ가 level1 −74% → budget100 −72% →
  d2 **−6.4%**(0.3647→0.3412). CNN이 좋아질수록 물리의 값어치가 추론 보정에서 신뢰도
  지표(라벨 없는 실패 지목) 쪽으로 이동한다. 이것 자체가 보고할 발견이다.
- **방향 결정 (사용자 합의)**: 디코더 주변 최적화는 종료(제거는 아님 — 되돌림·신뢰도 지표는
  추론 경로에 유지). 라운드 3 = d2 백본 위 부착 모듈(rFFT 분기·SE 어텐션, 근거는 각각
  freq_id 물리·EDA 채널 SNR 3배) + 깊이 연장 대조축(resnet-d4) — 모듈 Δ를 "그냥 더 깊게"의
  Δ와 같은 표에서 읽기 위해서다. 라운드 3이 마지막 GPU 라운드, 이후 Task 9.

### 라운드 3 (부착 모듈) — SE 압승, d4는 실행 중 범위 제외 (같은 날, Colab L4)

- **d2-se (+4.1% 파라미터): val 0.2954** (d2 대비 **−19%**, train_l1 0.2483) — CNN 단독으로
  디코더 역해 상한(0.3336)보다 아래다. **d2-fft (+0.5%): val 0.3589** (−0.0058, 실측 개선).
  둘 다 전체 holdout val 기준. post-LM·되돌림 후 정본 판정은 model.pt 회수 후 일괄
  (라운드 4 종료 시).
- **resnet-d4는 에폭 6에서 수동 중단 — 범위 제외 (사용자 결정).** se의 압승으로 깊이
  대조축(1.8시간)의 기대 정보 가치가 낮아졌다. **모듈 vs 깊이 직접 비교는 미측정으로
  남는다.** 부분 산출물은 git에서 제거, 미러 잔여 상태도 삭제 대상 (CHECKPOINTS.md).
- 세션이 push 셀 전에 중단돼 무결성 검증 셀 미실행 — 미러 검증은 최종 로컬 판정에서
  CNN MAE = 기록 val 재현으로 갈음 (라운드 1·2에서 쓴 것과 같은 대조 수단).
- **라운드 4 = d2-se-fft 1 run**: 두 모듈이 독립적으로 이겼고 작동 축이 다르므로
  (입력 표현 vs 채널 재가중) 결합을 시험한다. Δ는 **d2-se 대비**, 지면 채택 = d2-se.

## 08-20 (목) — Task 8 정본 판정: 물리 보정의 교차점을 지났다

- **전체 holdout 8-run 통합 표 확정** (`reports/task8_judge.md`, 표본 오차 0. CNN MAE =
  기록 val 재현으로 라운드 3·4 미러 검증 겸함): d2 계열 4종의 post-LM이 0.341~0.346으로
  수렴 — 디코더 계통오차 바닥(역해 상한 0.3336) 근처라 post-LM은 CNN 품질과 무관해졌다.
- **교차점**: d2-se는 **CNN 단독(0.2954)이 물리 보정 파이프라인(되돌림 후 0.3423)보다
  −13% 좋다** — LM이 계통오차 있는 디코더의 최적점으로 해를 끌고 가 처음으로 순손해.
  물리 보정의 값어치 붕괴(−74% → −6.4% → **음수**)가 완결된 곡선으로 남았다.
- **채택 후보 2 — 격자 밖 전이가 갈림길이고 리더보드만이 확정한다** (라벨 없는 점검 실측):
  - `d2-se` raw (holdout **0.2954**): test 예측 격자 거리 평균 2.32(기대 2.5 — 약한 격자
    끌림)이나 **재구성 잔차 꼬리가 test에서 p90 2.3배·p99 3배** — 격자 보간의 이점이
    raw CNN에는 test로 다 옮겨가지 않을 수 있다.
  - `d2-fft` + LM + 되돌림 (holdout **0.3396**): post-LM 잔차 분포가 holdout↔test
    p99.9까지 일치 — 전이 깨끗함 (budget100 제출에서 검증된 파이프라인 형태).
  - 제출 파일 두 벌 생성 완료 (`submission_d2-se.csv` · `submission_d2-fft_refined.csv`,
    git 미추적). 둘 다 업로드해 raw CNN vs 물리 파이프라인의 격자 밖 승부를 직접 잰다 —
    이 비교 자체가 교차점 발견의 out-of-distribution 검증이다.

## 08-20 (목) — Task 9 문서화: 최종 보고 서사로 재구성 (task9/docs)

- **루트 README 전면 재구성 — 슬림 쇼케이스 (약 130줄)** — 채용 심사자(반도체 도메인,
  비 AI 전공) 대상: 취지 → 접근 3항(구조 bias · 물리 디코더 · 물리의 자리는 추론) →
  결과(3자 비교 holdout/test 두 열 + 격자 밖 반전 + 자원 미터 + 한계) → 구조 → 시작하기.
  방법 상세는 싣지 않고 전부 reports/* 링크로 — 가독성이 우선이고 상세의 정본은
  리포트다. 수치는 전부 정본에서 복사 (CLAUDE.md 기록 규약에 예외 명시). 채용 어필용
  1-pager는 별도 아티팩트로 제작 (저장소 외부).
- **그림 2종 스크립트 산출** — `make_headline_figure.py` 확장: 사다리를 task8 채택 경로로
  연장(140배 대결) + 격자 밖 반전 slopegraph `fig_offgrid.png` 신설. test 수치의 기계가독
  정본으로 `reports/leaderboard.json` 신설 (외부 측정이라 손기록이 허용되는 유일한 수치
  파일 — 제출 확인 직후 갱신).
- **일관성 스윕에서 산술 오기 발견·정정**: 채택 파이프라인의 전이율은 0.3396 → 0.33895 =
  **−0.2%**다 (task8.md·CLAUDE.md·본 노트의 −0.02%는 계산 오류였다 — 그림 스크립트가
  정본에서 재계산하며 걸러냈다). `reports/README.md` 진행 비교표·읽기 순서 8편·파일 구분
  표에 task8 반영, CLAUDE.md 제출 명령 예시를 채택 run(`task8/d2-fft`)으로 갱신.

## TODO

체크박스 정본은 CLAUDE.md의 「작업 백로그」다. 여기에는 열린 항목의 **다음 행동**만 적는다.

- [x] **추론 지연 — GPU 정본 표 + 기본값 전환 완료.** L4에서 해석적+조기종료가 정확도 손실 0에
      15배 절감(17.41배 손해 → 1.30배), 기본값을 analytic+조기종료로 전환하고
      `inversion_refine.md` 재생성(수치 재현) · 게이트 (d)는 fd 명시 고정(diff 0).
      기록: 위 08-18 연표
- [x] **레시피 라운드 1 — 레버는 예산 하나였다.** 에폭 30 → 100이 post-LM 0.6110 → 0.4884,
      손잡이 4종(표준화·EMA·노이즈·꼬리)은 이득 없음(표준화는 2.4σ 손해). 확정 모델은
      `runs/cnn_recipe/budget100`. 취합: [reports/cnn_recipe.md](../reports/cnn_recipe.md)
- [x] **되돌림 규칙** — `budget100` 기준 0.4884 → **0.3880 nm**, 213M 단독(0.3955)을 넘었다.
      정본 `reports/cnn_recipe_judge.md`
- [x] **제출 경로 배선** — `--refine`으로 한 줄 생성, 잔차 전이 확인까지
- [x] **리더보드 업로드 — test MAE 0.38733 (15위).** holdout 0.3880이 격자 밖에서 열화 없이
      전이 (−0.2%, 극단 꼬리는 MAE에 반영 안 됨). 기록: 위 08-18 연표 +
      `reports/cnn_recipe.md` «리더보드 확정»
- [ ] **Task 8 — 모델·학습 최적화** (신설, 문서화는 Task 9로): 판정은 `judge_recipe.py`
      기준, Δ는 budget100 대조. 기각된 손잡이 4종 재시도 금지. 스냅샷 앙상블은 하지
      않는다(추론 지연을 다시 망친다). 다중 시작 재탐색은 하지 않기로 결정(08-18) —
      등가 분지 한계는 `reports/cnn_recipe.md` 「후속」에 기록.
  - [x] **라운드 1 (구조) 완료 + 전체 holdout 판정 (08-19)** — 두 구조 모두 대조군을 넘고
        되돌림 후 0.3838/0.3803으로 **프로젝트 최선 0.3880 경신** (매칭 0.66M). 승자 사다리로
        **라운드 2 구조 = resnet** 확정 (위 08-19 연표, 정본 `reports/task8_judge.md`).
  - [x] **라운드 2 (용량) 완료 (08-19)** — resnet-d2가 되돌림 후 **0.3412**로 최선 경신
        (실패율 0.08%), 깊이 ≫ 폭 (위 08-19 연표, 정본 `reports/task8_judge.md`).
        채택 확정·자원 미터("322배"→141배)·진행 비교표 갱신은 라운드 3 결과와 함께.
  - [x] **라운드 3 (부착 모듈) 완료 (08-19)** — d2-se val **0.2954**(−19%) · d2-fft 0.3589.
        d4는 실행 중 범위 제외 (위 08-19 연표).
  - [x] **라운드 4 (모듈 결합) 완료 (08-20)** — d2-se-fft val 0.2960 / 표본 post-LM
        Δ부모 −0.0003 = **동률**. 사전등록 2에 따라 **채택 후보 = d2-se** (더 단순한 쪽).
        fft의 이득은 se 위에서 남지 않는다 — 채널 재가중이 주파수 표현의 몫을 이미 덮는다.
  - [x] **전체 holdout 정본 판정 + 제출 파일 2벌 (08-20)** — 교차점 확정, 채택 후보
        d2-se(raw) vs d2-fft(+LM+되돌림) (위 08-20 연표).
  - [x] **리더보드 확정 (08-20)** — `submission_d2-fft_refined.csv` **test MAE 0.33895
        (4위)**: holdout 0.3396이 격자 밖에서 열화 없이 전이 (−0.2%). raw CNN
        `submission_d2-se.csv`는 **0.5461로 붕괴** (holdout 0.2954 대비 +85%) — 라벨 없는
        잔차 꼬리 경고(p90 2.3배)가 예측한 그대로, holdout의 raw CNN 이점은 격자 보간
        인공물이었다. **채택 = d2-fft + LM + 되돌림.** 물리 보정의 값어치가 격자 밖
        강건성으로 재정의된 것이 Task 8의 최종 발견이다.
  - [x] **자원 미터 벤치 (라운드 5, 08-20)** — 정본 `reports/task8_bench.md` (Colab 한
        세션, CPU·L4). 배포 경로 합계 = skip-MLP 대비 CPU 1.42배 · L4 1.49배 (d2-fft
        forward 단독은 213M보다 빠름 — L4 0.39×). 파라미터 140배 · 체크포인트 133배.
  - [x] **Task 8 종결 (08-20)** — `reports/task8.md` 취합(교차점·격자 밖 반전·자원 미터·
        사전등록 대비) + CHECKPOINTS(task8 절) + CLAUDE.md(백로그·확정 스펙·하지 말 것
        1건 추가: raw CNN 무검증 제출 금지 + 잔차 관문).
  - [x] **3자 test 비교 (완결, 08-20)** — 사전등록: ① d2-fft raw 열화 예상(관문 신호
        p90 1.95배) ② 213M raw도 관문 실측 후 예측 기록 → 리더보드 검증.
        **① 적중: d2-fft raw test 0.5911 (+65%)** — 관문 신호와 열화가 단조
        (1.95배→+65% · 2.3배→+85%).
        **② 절반 빗나감: 213M test 0.4752 (+20%)** — 관문(꼬리 1.03배) 통과였는데
        열화했다. "채택이 test에서도 이긴다"는 적중 (0.33895 vs 0.4752, **−29%** —
        holdout −14%보다 큰 격차). 교훈을 규칙으로: **관문은 격자 끌림 붕괴 검출용이고
        통과 ≠ 무열화** (잔차는 노이즈 바닥 아래 sub-nm 보간 오차에 둔감, 감도 ~0.01/nm)
        → raw 제출 자체를 금지로 격상 (CLAUDE.md 「하지 말 것」 갱신). **3자 표 완성**:
        측정한 raw 신경망 셋 전부 격자 밖 열화(+20~85%), 물리 파이프라인만 전이(−0.2%)
        — 정본 `task8.md` 발견 ② 표.
- [x] **Task 9 — 문서화 (08-20 완료)**: 루트 README를 최종 보고 서사(6절 + 방법 상세)로
      재구성, `reports/README.md` 진행 비교표·읽기 순서에 task8 반영, 헤드라인 그림
      재생성 + 격자 밖 반전 slopegraph 신설, `reports/leaderboard.json`(제출 실측의
      기계가독 정본) 신설, 일관성 스윕 (아래 08-20 연표).
- [x] **평가 축 실측** — `reports/cnn_recipe_axes.md` (budget100 + flatten-dilated-bound,
      holdout 전체). 균등·가우시안이 같은 총 σ에서 거의 같은 열화, 신뢰도 ρ 0.70
- [x] **`reports/stage_b.md` 취합** — 세 라운드 관통 서사, 표는 curves 3종 참조
- [ ] **Stage A 게이트 (b) 후속**: 남은 레버는 물성 모델 형태뿐이다 (근거·후보 순서는
      week_1.md 08-13). 마감 안에서는 진행하지 않는다 — 한계로 기록 (stage_b.md §6,
      cnn_recipe.md 「후속」)
- [x] **위생**: `verify_data.py --deep` — test 표본 64행 전부 최근접 train 대비 max|ΔR|
      0.0473~0.1395 > 임계 0.0304 (격자 밖 반증이 스크립트 경로로 재현됨)
