# stage_a — Si 표 대안: Schinke 2015. **Stage B 물리 디코더로 채택된 설정.**
#
# `joint-lam3-sin2-si2`와 **si_source만** 다르다. 세 표를 전부 적합해 네 지표(RMSE·계통
# 오차·위반율·역해 MAE)가 같은 순서로 정렬됨을 확인하고 측정으로 골랐다.
seed: 42
experiment: stage_a
run_name: joint-lam3-sin2-si2-schinke

data:
  fit_rows: 8000
  diag_rows: 20000

model:
  si_source: Si_nk_Schinke.yml
  free: [lam_nu0, lam_r1, lam_r2, sin_b1, sin_c1, si_de, si_klog]
