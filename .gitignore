# --- 대회 데이터 (재배포 금지 — CLAUDE.md 데이터 계약) ---
# 파일은 무시하고 디렉토리 구조(.gitkeep)만 추적한다.
# 선행 슬래시로 저장소 루트에 고정한다. 앵커가 없으면 `src/data/` 패키지까지
# 함께 무시되어 소스가 커밋에서 누락된다.
/data/**
!/data/*/
!/data/**/.gitkeep
# runs/는 전체 추적한다 (model.pt, train.log, metrics.json — 대실험별 취합 리포트는 reports/)

# --- 파이썬 환경 ---
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.ipynb_checkpoints/

# --- 캐시된 데이터셋 ---
*.parquet

# --- OS ---
.DS_Store
